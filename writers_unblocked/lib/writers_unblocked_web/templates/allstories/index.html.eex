<!--
<div>
  <%= case Map.fetch(assigns, :statmsg) do {:ok, msg} ->  "submission info" <> msg <> "\n\n"; _ -> "" end %>
</div>
-->

<div class="center">
	<%= for {story, n} <- Enum.with_index(@allstorieslist) do %>
		<h3>Story #<%= n + 1 %>: <%= List.first(story) %></h3>
		<%= text_to_html List.last(story) %>
  	<% end %>
</div>