<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">

    <title>WritersUnblocked</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    
    <script type="text/javascript">
      // Put CSRF token in a variable so scripts can access it for post requests
      var CSRF_TOKEN = <%= raw Poison.encode!(Plug.CSRFProtection.get_csrf_token()) %>
    </script>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
    <script>require("js/app").App.run()</script>
  </head>

  <body>
    <div class="container" id="main-content">
      <header class="header">
        <a href="/" id="main-heading">
            <h1>Writers Unblocked</h1>
        </a>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
  </body>
</html>
